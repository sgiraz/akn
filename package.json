{
  "name": "akn",
  "version": "1.0.0",
  "description": "Generates an Arkanoid-style animated SVG from a GitHub contribution graph",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --outfile=dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^1.10.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  },
  "license": "MIT"
}
